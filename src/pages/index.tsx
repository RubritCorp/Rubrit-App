//components
import Layout from "components/layout";
//chakra
import { Grid, GridItem } from "@chakra-ui/react";

const index = () => {
  return (
    <Layout title={"Rubrit"} description={"Landing Page"}>
      landing
    </Layout>
  );
};

export default index;
